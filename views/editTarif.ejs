<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Form Edit Tarif</title>

  <!-- Bootstrap -->
  <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css">

  <!-- Font Awesome -->
  <link rel="stylesheet" href="/fontawesome/css/all.min.css">
</head>

<body class="bg-light">

  <div class="container my-5">
    <div class="row justify-content-center">
      <div class="col-lg-8">

        <div class="card shadow-sm border-0">
          <div class="card-body">

            <h4 class="fw-bold mb-4 text-center">Edit Tarif</h4>

            <form action="/tarif/edit/<%= tarif._id %>" method="POST">

              <div class="mb-3">
                <label class="form-label fw-semibold">Jenis Kendaraan</label>
                <select name="jenis_kendaraan" class="form-control">
                  <option value="motor" <%= tarif.jenis_kendaraan === 'motor' ? 'selected' : '' %>>
                    Motor
                  </option>
                  <option value="mobil" <%= tarif.jenis_kendaraan === 'mobil' ? 'selected' : '' %>>
                    Mobil
                  </option>
                </select>
              </div>

              <div class="mb-4">
                <label class="form-label fw-semibold">Tarif per Jam</label>
                <input
                  type="number"
                  class="form-control"
                  name="tarif_per_jam"
                  value="<%= tarif.tarif_per_jam %>"
                />
              </div>

              <button type="submit" class="btn btn-warning btn-lg w-100 fw-semibold">
                Simpan Perubahan
              </button>

            </form>

          </div>
        </div>

      </div>
    </div>
  </div>

</body>
</html>
